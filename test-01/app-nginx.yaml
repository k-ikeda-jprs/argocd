apiVersion: apps/v1
kind: Pod
metadata:
  name: test-01-app
  labels:
    name: test-01-app
spec:
  containers:
  - name: nginx
    image: nginx:1.17.1
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"